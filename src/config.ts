/**
 * Datenpfad – relativ zur Vite-base.
 * CSV: public/data/vnb.csv
 * Optional JSON mit denselben Feldern: public/data/vnb.json (wird bevorzugt, falls vorhanden)
 */
export const DATA_BASE = 'data/vnb';
export const DATA_CSV_URL = `${DATA_BASE}.csv`;
export const DATA_JSON_URL = `${DATA_BASE}.json`;
